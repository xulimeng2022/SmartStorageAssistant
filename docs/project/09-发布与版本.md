# 智能收纳助手 · 发布与版本

> 版本事实从 `app/build.gradle.kts` 与 Git 历史核对；无法核对的标「待确认」。详细操作见根目录 `RELEASE_GUIDE.md`。

## 当前版本
- versionName：1.1.0；versionCode：4（`app/build.gradle.kts` defaultConfig）
- 代码基线：main 已与 `codex/release-v1.1.0`（7c6a0a7）对齐

## 版本历史（本地可核对的记录）
| 版本 | versionCode | 内容摘要 | 证据 |
| --- | --- | --- | --- |
| v1.0.0 | 待确认（曾“重置版本号为 1.0.0”，推测 1，待从 git 历史核对） | 首次发布 | git 历史 + release/v1.0.0/mapping.txt |
| v1.0.1 | 2 | 搜索逐字模糊、多图附件与全屏照片预览、AI 批量识别 | `app/build.gradle.kts` 历史 + release/v1.0.1/mapping.txt |
| v1.1.0 | 4 | 三语言切换、微信式深色、AI 解析失败两出口引导、批量照片归属修复、全屏照片缩放预览、LLM 错误分类 | 当前配置 + README 更新记录 + release/v1.1.0/mapping.txt |

> versionCode 从 2 直接到 4（无 3）：中间是否有未发布构建，待确认。

## 版本规则
- 仓库 AGENTS.md「版本号自动递增规则（强制）」：需求/修复/重构/UI/配置/依赖等实际修改项目文件后自动递增 `versionCode`（每次 +1）；`versionName` 至少递增补丁号；仅改文档/注释不触发。
- 每次发版 versionCode 必须大于上一版（Android 升级判定）。

## 构建
| 类型 | 命令 | 产物 |
| --- | --- | --- |
| Debug | `.\gradlew.bat :app:assembleDebug` | `app\build\outputs\apk\debug\app-debug.apk` |
| Release | `.\gradlew.bat :app:assembleRelease` | `app\build\outputs\apk\release\app-release.apk` |

- Release 开启 R8 混淆与资源压缩（`isMinifyEnabled` / `isShrinkResources`），规则在 `app/proguard-rules.pro`。

## 签名（只记录位置与规则，不记录值）
- 签名配置在 `app/build.gradle.kts` 的 release 构建类型，从根目录 `local.properties` 读取 `KEYSTORE_FILE / KEYSTORE_PASSWORD / KEY_ALIAS / KEY_PASSWORD`；未配置则产出未签名 APK。
- 密钥库文件（`*.jks`/`*.keystore`）已被 `.gitignore` 忽略，严禁提交；`local.properties` 同样不入库。
- 密钥丢失后无法对已发布 App 升级签名，需妥善备份（见 RELEASE_GUIDE.md）。

## Release 流程（GitHub Releases）
1. 更新 `versionName/versionCode`；构建 Release APK；留存 `mapping.txt`。
2. 归档混淆映射到 `release/<版本>/mapping.txt`（仓库现有 v1.0.0/v1.0.1/v1.1.0）。
3. 在 `https://github.com/xulimeng2022/SmartStorageAssistant` 创建 Release（Tag 形如 `v1.1.0`），上传 APK。
4. 更新 README「更新记录」。
- 分支习惯：历史经 `codex/release-vX.Y.Z` 分支发布后并入 main（v1.0.0、v1.1.0 均如此）。

## 发布检查清单（发布前）
- [ ] 版本号已递增且大于上一版
- [ ] 单元测试通过（`:app:testDebugUnitTest`）
- [ ] Release APK 构建成功
- [ ] 无敏感信息（签名/Key）进入产物与仓库
- [ ] Release Notes 已写；README 更新记录已同步
- [ ] 真机安装验证（混淆后测试清单见 RELEASE_GUIDE.md，执行结果记录到 04 T-005）
- [ ] Git 状态干净、Tag 与发布物一致（T-004）
