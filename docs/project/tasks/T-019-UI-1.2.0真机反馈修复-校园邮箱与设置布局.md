# T-019 UI 1.2.0 真机反馈修复：校园邮箱与设置布局

## Multi-Codex Metadata

- Task ID: T-019
- Owner: UI
- Risk: Medium
- Source Branch: `codex/ui`
- Behavior Baseline: `codex/integration@880dfa97c0519c9e336aa19a8d1530846b58c72f`
- Contract Change: 无；仅 UI 内部数据模型、Compose 布局和 UI 资源
- Cross-module Request: 无
- Handoff Target: 主控（Coordinator）

## Task Boundary

- 所属模块：M11、M12。
- 目标行为：把关于页“邮箱”标签改为“校园邮箱”，并修复数据管理设置项长说明挤压标题的问题。
- 允许修改：`presentation/settings/**`、`presentation/about/**`、UI localization resources、UI tests。
- 禁止修改：Room/DAO/Repository、`data/**`、`domain/**`、AI contract、版本号、Gradle 依赖、中央协调文档及其他角色 Role State。
- 不新增固定三语标签专项测试；沿用现有 localization resource 检查。
- 需要界面布局验证时使用真实设备；无设备时明确标记 `NOT VERIFIED`，不得用编译结果替代。

## Required Fixes

### UI-5 校园邮箱标签

- `about_email` 简体中文改为“校园邮箱”。
- 繁體中文改为“校園信箱”。
- English 改为 `University email`。
- `xulimeng2026@hnu.edu.cn` 保持不变，显示、复制和打开邮件应用继续使用同一地址。
- 其他联系方式、跳转行为和邮箱复制兜底文案不变。

### UI-6 设置项公共布局

- 为 `SettingsItem` 增加可选 `subtitle`；`value` 仅承载右侧短状态，例如版本号、当前语言。
- 主行布局固定为：左侧图标；中间 `weight(1f)` 的标题/副标题列；可选右侧短状态；最右侧箭头。标题和说明必须自然换行，不得被右侧内容挤成逐字竖排。
- 保持整行点击、禁用态、分隔线、附加 `content` 和自定义 `trailingIcon` 行为。
- 将以下长说明从 `value` 迁移到 `subtitle`：
  - 回收站 `trash_desc`
  - 导出数据 `export_desc`
  - 导入数据 `import_desc`
  - 完全删除 App 数据 `settings_reset_all_desc`
  - 搜索小贴士 `settings_searchtips_value`
  - 赞助支持 `donate_desc`
- 其他使用 `SettingsItem` 的页面必须继续编译且行为不变。

## TDD / Verification

- 先运行现有 `LocalizationRegressionTest` 作为资源对齐回归基线，不新增固定标签专项测试。
- 完成改动后运行相关 JVM 测试和 Debug 构建；中文路径触发既有 classpath 问题时按 `README.md` 的 ASCII 临时目录规则处理。
- 界面检查矩阵：当前手机宽度、320dp 窄屏、1.5 倍字体、简体/繁體/English、深色模式。
- 检查“完全删除 App 数据”只打开确认入口，不执行真实清空。
- 无设备时布局和截图标记 `NOT VERIFIED`，并提供人工复测步骤。
- 不 merge、不 rebase、不同步其他角色分支。

## Acceptance Criteria

- [ ] 三语邮箱标签和地址要求满足，其他联系方式行为不变。
- [ ] `SettingsItem` 的标题、副标题、短状态和箭头布局符合要求。
- [ ] 数据管理及同组件长说明行在窄屏和大字体下不发生逐字竖排、重叠、截断。
- [ ] 现有 JVM/localization 检查与 Debug 构建结果已记录。
- [ ] 没有修改 Owner 范围外文件。
- [ ] 输出实现 commit、docs Handoff commit、实际修改文件和未验证项。

## Dispatch

- Project: 智能收纳助手 - UI
- projectId: `5881b1c0-6300-490d-bd2f-2862483e0a67`
- Worktree: `C:\Users\XU\.codex\worktrees\f98f\智能收纳助手`
- Branch: `codex/ui`

## Fallback / Single Writer

- 目标为既有 UI 永久工作树；不创建新 Worktree。
- 永久工作树不可用时记录 `PERMANENT_WORKTREE_FALLBACK` 并停止，不由 Coordinator 或临时子 Agent 冒充 UI 实现。