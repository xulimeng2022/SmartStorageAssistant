# T-013 v1.2.0 RC 专项回归核验与缺陷收口

## Multi-Codex Metadata

- Task ID: T-013
- Owner: 主控（Coordinator）
- Risk: High
- Source Branch: `codex/integration`
- Verification Baseline: `9afd50c02638aa9804924a300957679123868c3d`
- Candidate: `app/release/智能收纳助手-1.2.0-rc.apk`
- Candidate SHA-256: `A3D306334E1BC57874378DEF93FC6F1534515CFB2FCC75F00110E90D76E8EFE2`
- Contract Change: 预计无；若核验发现必须变更 public contract、Room schema 或跨模块接口，停止修改并回到 Coordinator 重新划分 Single Writer。
- Cross-module Request: UI / AI / Data 只读专项核验；首轮不授予业务代码写权限。
- Execution Order: 冻结基线 → UI / AI / Data 永久工作树只读核验 → Coordinator 汇总分类 → 缺陷按临时 Single Writer / Task Card 返修 → 重新验证 → 真机验收。
- Handoff Target: 主控（Coordinator）

## Task Boundary

- 所属模块：M01、M03、M04、M05、M06、M08、M09、M10、M11、M12。
- 目标行为：对 T-011 的 1.2.0 RC 实现进行专项回归核验，确认高风险数据、索引、合并、清空和版本检查链路没有可复现缺陷；只修复有证据且经 Coordinator 分类的问题。
- 本任务不做：新增功能、正式版本号变更、Push、Tag、正式 Release、真机结论伪造、Legacy Worktree 清理、无关重构、依赖升级或重新创建 Worktree。
- 前置依赖：T-011 已进入 `codex/integration`；Coordinator / Data / AI / UI 永久工作树可用；候选 APK 哈希与任务卡记录一致。

## 角色核验合同

### 通用规则

- Target: 既有 UI / AI / Data 永久工作树 Project；不得创建新 Worktree。
- 首轮 Mode: `READ_ONLY`。不得 checkout、merge、rebase、reset、commit、修改业务代码或 Role State。
- Baseline: 以 `codex/integration@9afd50c02638aa9804924a300957679123868c3d` 的集成结果为准；角色当前分支可能不同，不得自行同步。
- 证据：实际读取、命令结果、文件路径与行号、测试名与退出码；无法验证的内容标记 `NOT VERIFIED`。
- Findings: 按 `BLOCKER / IMPORTANT / OPTIONAL` 分类，至少包含证据、影响范围、最小复现或检查步骤、建议 Owner。
- Required Output: 实际 cwd、branch、HEAD、核验前后 `git status --short`、findings、验证命令与结果、未覆盖风险。
- 写操作：任何代码修复都必须等待 Coordinator 建立返修 Task Card / 临时 Single Writer，不接受“顺手修复”。

### UI 专项核验

- 范围：单图图片索引创建/删除交互、批量合并预览与照片规则、检查更新三态、完全删除数据两步确认与范围说明、校园邮箱兜底、三语言资源和深色模式。
- 重点：状态不会把失败显示为成功；删除/清空提示不可恢复；未安装邮件应用时有复制兜底；字符串无硬编码遗漏。
- 禁止：修改 `presentation/**`、res、ViewModel、测试或其他文件。

### AI 专项核验

- 范围：`VoiceTextNormalizer`、`LocalDescriptionParser`、图片索引 Worker / Coordinator、`requested` / `generation` 防回写、视觉未授权与能力探测降级。
- 重点：旧任务回写、取消/删除竞态、无授权上传、错误分类与本地结果回退；复核 AI 侧测试是否覆盖关键状态转换。
- 禁止：修改 `data/remote/llm/**`、`data/remote/vision/**`、AI 测试或其他文件。

### Data 专项核验

- 范围：Room v4→v5→v6 迁移、导入暂存/事务/图片文件时序、数据库提交后清理、`AppDataResetRepository`、备份 1.0/1.1 兼容、图片索引软删除与永久删除。
- 重点：迁移不清空历史数据；提交失败不留孤儿文件或错误关联；完全删除不触碰系统相册和外部导出；清空后后台任务不会回写。
- 禁止：修改 Room、DAO、Migration、Repository、prefs、备份、测试或其他文件。

## Acceptance Criteria

- [ ] 冻结基线 commit、候选 APK 路径与 SHA-256 在任务卡中记录并复核。
- [ ] UI / AI / Data 三个核验线程均返回 threadId、实际 Project、cwd、branch、HEAD、核验前后 `git status --short`。
- [ ] 三个角色分别输出分类 findings 和 `NOT VERIFIED` 清单，不把代码存在当作行为已通过。
- [ ] Coordinator 对 findings 完成分类；BLOCKER 必须闭环，IMPORTANT 必须修复或有书面风险接受，OPTIONAL 进入后续任务池。
- [ ] 确认需要返修时，按对应 Owner 的永久工作树投递，不允许 Coordinator 或 `spawn_agent` 冒充模块实现。
- [ ] 修复后在 `codex/integration` 重新运行受影响测试和 Debug/Release 构建；真机项继续标记 `NOT VERIFIED`。
- [ ] 不修改正式版本号，不 Push、不 Tag、不正式发布。
- [ ] 最终形成 T-013 Completion Conclusion 和真机验收输入。

## TDD / Verification Record

本任务属于高风险候选包的核验与缺陷收口；首轮以最小复现和只读审查为主。确认缺陷后，修复必须按对应模块补充或更新可执行的失败回归测试，再最小实现。

| 验证项 | 结果 | 证据 |
| --- | --- | --- |
| Baseline / Candidate Freeze | PASS | `codex/integration` HEAD 与 RC SHA-256 已由 Coordinator 实测 |
| UI 专项核验 | PENDING | 待 UI 永久工作树结果 |
| AI 专项核验 | PENDING | 待 AI 永久工作树结果 |
| Data 专项核验 | PENDING | 待 Data 永久工作树结果 |
| Coordinator 汇总分类 | PENDING | 待三方结果 |
| 缺陷返修与回归 | PENDING | 无已确认 findings 前不创建代码任务 |
| JVM / Debug / Release 复核 | PENDING | 返修后执行；无返修时复用 T-011 证据并记录复用理由 |

## Permanent Worktree Dispatch Evidence

| Target Role | Actual Project | threadId | Expected Worktree / Branch | `git rev-parse --show-toplevel` | `git branch --show-current` | Before / After Status | Result |
| --- | --- | --- | --- | --- | --- | --- | --- |
| UI | 智能收纳助手 - UI | PENDING | `f98f` / `codex/ui` | PENDING | PENDING | PENDING | PENDING |
| AI | 智能收纳助手 - AI | PENDING | `ae14` / `codex/ai` | PENDING | PENDING | PENDING | PENDING |
| Data | 智能收纳助手 - Data | PENDING | `95b0` / `codex/data` | PENDING | PENDING | PENDING | PENDING |

## Implementation Log

- 2026-09-13：建立 T-013，冻结 T-011 集成基线与 RC 候选包，准备向 UI / AI / Data 永久工作树投递只读专项核验。

## Fallback Record

- `PERMANENT_WORKTREE_FALLBACK`: 待投递结果。

## Completion Conclusion

- Status: IN PROGRESS
- Deliverable: 待三方核验结果与 Coordinator 分类结论
- Long-term facts to backwrite: 待定
- Follow-up: 用户真机覆盖升级与完整交互验收