# T-021 UI 1.2.0 最终 App 图标替换

## Multi-Codex Metadata

- Task ID: T-021
- Owner: UI
- Risk: Medium
- Source Branch: `codex/ui`
- Behavior Baseline: `codex/ui@1d9a7f48d337f6091b745ae6f6ed7a8c092f5f7a`
- Contract Change: 无
- Cross-module Request: 无
- Handoff Target: 主控（Coordinator）

## Task Boundary

- 所属模块：M07。
- 目标行为：使用用户提供的最终 PNG 原图，替换 Android Launcher、adaptive、round 与 Android 12+ 默认 splash 的图标来源。
- 允许修改：`app/src/main/res/**`、UI Role State、M07。
- 禁止修改：业务逻辑、AI/数据库、依赖、版本号、网站、Archify、Manifest（本任务无需改动）。
- 不重新设计图标；不修改立方体、星光、原始渐变颜色或主体比例。

## Visual Baseline

- 原图：`D:\Users\XU\Downloads\aa54b516-b218-4ca5-8bd1-17812c44d318.png`
- 尺寸：1254×1254
- SHA-256：`4D32F0B84D8963EDF121BDFB01EE20951A755E76AA21C65486C11F5CBDCA94BC`

## Implementation

- 新增 `drawable-nodpi/ic_launcher_background_image.png`：同源蓝紫渐变的全幅背景。
- 新增 `drawable-nodpi/ic_launcher_foreground_image.png`：从原图分离白色立方体、星光和主体明暗，透明背景。
- `mipmap-anydpi-v26/ic_launcher.xml` 与 `ic_launcher_round.xml` 同时引用新 background / foreground。
- Android 12+ 未声明独立 splash 资源，系统默认 splash 跟随 launcher icon；本任务不新增 SplashScreen API 主题。
- 保留 `AndroidManifest.xml` 的 `android:icon` / `android:roundIcon` 引用不变。
- 构建通过后确认无旧引用，再删除 `drawable/ic_launcher_foreground.xml` 与旧 `ic_launcher_background` color。

## Verification

- Resource processing：`:app:processDebugResources` PASS。
- JVM：ASCII 临时目录中 22 suites / 107 tests，0 failures / 0 errors / 0 skipped。
- Debug：ASCII 临时目录中 `:app:assembleDebug` PASS。
- Release / R8：ASCII 临时目录中 `:app:assembleRelease` PASS；为未签名 APK。
- Lint：`:app:lintDebug` 运行完成；仅既有 Manifest 相机硬件声明 error，114 warnings；无图标资源 error。
- APK 资源检查：Manifest icon / roundIcon、两个 v26 adaptive XML、两个 nodpi PNG 均已打包，XML 引用正确。
- 遮罩预演：圆形与圆角方形 mask 下无白色预圆角边缘、无主体明显裁切，星光保留。
- Android 真机：Launcher、圆形 / Squircle mask、Splash、最近任务、应用信息、App 内 Logo 均 NOT VERIFIED。

## Device Acceptance Steps

1. 覆盖安装或卸载重装新构建，检查桌面 Launcher 图标。
2. 切换系统圆形 / Squircle / 方形等 mask（若 OEM 支持），检查白色立方体与右上星光。
3. 冷启动检查 Android 12+ splash，以及设置 → 应用 → 智能收纳助手、最近任务页图标。
4. 检查 App 内是否存在旧品牌 Logo 或文案资源；当前扫描未发现。
5. 若仍显示旧图标，卸载旧 App → 重新安装 → 必要时清理 Launcher 缓存。

## Acceptance Criteria

- [x] Launcher、adaptive、round 均指向最终原图派生的新资源。
- [x] Splash 图标来源已确认；无独立 splash 资源需要修改。
- [x] 旧 vector / color 资源无引用后删除。
- [x] 资源编译、Debug、Release/R8 与 107 JVM tests 通过。
- [x] APK 内资源与 XML 引用已核验。
- [x] 未修改业务、依赖、版本号、网站、Archify，未 push / tag / release。
- [ ] 真机 Launcher / mask / splash 视觉待用户验收。

## Handoff

- UI implementation commit: `a378a32`
- UI docs Handoff: `a159c94`
- Integration implementation: `2b4a616`
- Integration docs Handoff: `e0bd8a9`
- Push / Tag / Release: 未执行。